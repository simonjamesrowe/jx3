filepath: ""
environments:
  default:
    values:
    - jx-values.yaml
namespace: mongodb
repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami
releases:
- chart: ./secrets
  version: 1.0.0
  name: mongo-secrets
  namespace: mongodb
  values:
  - jx-values.yaml
- chart: bitnami/mongodb
  version: 7.14.0
  name: mongodb
  values:
  - jx-values.yaml
templates: {}
renderedvalues: {}
