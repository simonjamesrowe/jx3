filepath: ""
environments:
  default:
    values:
    - jx-values.yaml
namespace: sonarqube
repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami
releases:
- chart: ./secrets
  version: 1.0.0
  name: sonarqube-secrets
  namespace: sonarqube
  values:
  - jx-values.yaml
- chart: bitnami/sonarqube
  version: 0.1.6
  name: sonarqube
  values:
  - values.yaml
  - jx-values.yaml
templates: {}
renderedvalues: {}
